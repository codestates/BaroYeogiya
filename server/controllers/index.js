const userController = require('./users');
const mapController = require('./maps');
const reviewController = require('./reviews');
const storeController = require('./stores');

module.exports = { userController, mapController, reviewController, storeController };