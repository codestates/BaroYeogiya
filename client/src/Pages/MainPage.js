import '../Css/MainPage'

function MainPage() {
    return (
        <div className="main-page-box">
            <button>로그인</button>
            <button>회원가입</button>
            <button>지도</button>
        </div>
    )
};

export default MainPage;